# hippocampus-docs

The documentation home of the **HippoCampusRobotics** lab: setup guides (migrated page
for page from the old Sphinx site), the team's projects (every org repository, grouped
and explained), in-browser search over code and CAD (phase 2), and the lab's agent tools.

**Zero-build static site.** No framework, no npm, no build step. The one vendored library
is `js/marked.min.js` (marked v12.0.2, MIT, sha256 pinned in `tools/check.py`).

## Run it

```sh
./tools/serve.sh          # serves on http://localhost:8130
```

GitHub Pages serves the same files byte-identical — there is nothing to build.

## Edit it

| To change… | Edit… |
|---|---|
| A setup page | `content/setup/<group>/<page>.md` |
| Setup structure/nav | `data/setup.json` (regenerated by the converter — prefer re-running it) |
| A project | `data/projects.json` (facts) + `content/projects/<id>.md` (prose) |
| An agent-tool page | `data/tools.json` + `content/tools/<id>.md` |
| Home/About | `data/site.json` / `content/about.md` |

Then run the gate — it must pass before every commit:

```sh
python3 tools/check.py
```

It validates strict-JSON registries, file existence both ways (no orphans), internal
links, image references, the setup parity counts, and the projects-coverage rule (every
org repo in exactly one project). Never weaken a check to get to green — fix the content.

## Where things come from

- `tools/rst_convert.py` performed the one-shot RST→Markdown migration from a clone of
  `HippoCampusRobotics/docs`; the page-for-page account (71 migrated, 26 dropped with
  reasons) is `docs/setup-parity.md`. Re-running the converter regenerates
  `content/setup/`, `assets/setup/`, `data/setup.json`, and the parity table.
- `data/org-repos.json` is the org snapshot the projects coverage is checked against;
  `data/cad-tree.json` is the canonical CAD repo's file list (search input, phase 2).
- `docs/site-plan.md` is the architecture record; `PROGRESS.md` the phase log.

## Status

**Live (staging): <https://kyle-nelson-berkeley.github.io/hippocampus-docs/>** —
published 2026-08-28 with Kyle's approval; `noindex` while staging. Search runs on the
committed precomputed index (`search/`, 75 shards, 0.41 MB; 10/10 acceptance probes at
rank 1 on the live URL — `data/search-probes.json`). Agent editing goes through the
private onboarding repo's `hippo-site` MCP server
(github.com/kyle-nelson-berkeley/hippocampus-team-onboarding, private). The org
cut-over (pointing the team at this URL) remains a team decision.
